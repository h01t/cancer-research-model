# FixMatch probe configuration with rescue augmentation but static SSL schedules.

dataset:
  data_dir: "data"
  abnormality_type: "mass"
  image_size: 512
  labeled_subset_size: null

model:
  name: "efficientnet-b0"
  num_classes: 2
  pretrained: true
  dropout_rate: 0.2
  freeze_backbone: true
  freeze_backbone_epochs: 5

training:
  batch_size: 8
  num_epochs: 150
  learning_rate: 0.001
  weight_decay: 0.0001
  momentum: 0.9
  optimizer: "adam"
  scheduler: "cosine"
  warmup_epochs: 5
  early_stopping_patience: 25
  num_workers: 20
  use_amp: true
  gradient_clipping: true
  max_grad_norm: 1.0
  class_weighted_loss: true

ssl:
  method: "fixmatch"
  confidence_threshold: 0.95
  confidence_threshold_start: 0.95
  confidence_threshold_end: 0.95
  confidence_threshold_ramp_epochs: 0
  lambda_u: 1.0
  lambda_u_start: 1.0
  lambda_u_end: 1.0
  lambda_u_ramp_epochs: 0
  unlabeled_batch_ratio: 2
  randaugment_n: 2
  randaugment_m: 10
  use_ema: true
  ema_decay: 0.999
  distribution_alignment: false

augmentation:
  weak:
    random_horizontal_flip: true
    random_vertical_flip: false
    random_rotation: 5
    color_jitter: 0.0
  mild_strong:
    random_horizontal_flip: true
    random_rotation: 8.0
    translate: [0.05, 0.05]
    brightness: 0.08
    contrast: 0.12

logging:
  save_freq: 10
  log_freq: 10

wandb:
  enabled: false
  project: "ssl-mammography"
  run_name: null
